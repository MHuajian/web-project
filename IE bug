IE 常见bug及解决方案

盒模型 bug   使用严格doctype声明

双倍margin bug（左浮元素，左margin是定义的2倍 右浮元素，右margin是定义的2倍 ）   _display:inline;

不认识a:link     不加:link
 
3像素margin bug   规范浮动与清除浮动

父元素下有俩个子元素 上面一个子元素是标准文档流 下面一个是完全脱离标准文档流 出现下面的div块 不会覆盖在上面的div上 

解决方法：设置上面那个div （display：block ） 
